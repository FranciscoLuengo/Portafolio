<section id="skills" class="py-20 overflow-hidden">
    <div class="container mx-auto px-6">
        <h2 class="text-3xl font-bold tracking-tighter sm:text-4xl md:text-5xl mb-8 bg-gradient-to-r from-white to-blue-600 bg-clip-text text-transparent text-center leading-snug">
            Tecnologías
        </h2>

        <p class="text-muted-foreground md:text-lg leading-relaxed mb-12 text-blue-100 text-center">
            Herramientas y tecnologías que domino
        </p>

        <div class="relative max-w-6xl mx-auto">
            <div class="absolute left-0 top-0 bottom-0 w-32 bg-gradient-to-r from-[#0a0a0a] to-transparent z-10 pointer-events-none"></div>
            <div class="absolute right-0 top-0 bottom-0 w-32 bg-gradient-to-l from-[#0a0a0a] to-transparent z-10 pointer-events-none"></div>

            <div class="carousel-container">
                <div class="carousel-track">
                    <div class="carousel-row" data-direction="right">
                        <div class="tech-card">
                            <img class="w-16 h-16 object-contain" src="assets/logos/angular-original.svg" alt="angular logo" />
                            <span class="text-sm font-medium text-white mt-2">Angular</span>
                        </div>
                        <div class="tech-card">
                            <img class="w-16 h-16 object-contain" src="assets/logos/react-original.svg" alt="react logo" />
                            <span class="text-sm font-medium text-white mt-2">React</span>
                        </div>
                        <div class="tech-card">
                            <img class="w-16 h-16 object-contain" src="assets/logos/nextjs.svg" alt="next logo" />
                            <span class="text-sm font-medium text-white mt-2">Next.js</span>
                        </div>
                        <div class="tech-card">
                            <img class="w-16 h-16 object-contain" src="assets/logos/tailwindcss-original.svg" alt="tailwind logo" />
                            <span class="text-sm font-medium text-white mt-2">TailwindCSS</span>
                        </div>
                        <div class="tech-card">
                            <img class="w-16 h-16 object-contain" src="assets/logos/typescript-original.svg" alt="typescript logo" />
                            <span class="text-sm font-medium text-white mt-2">TypeScript</span>
                        </div>
                        <div class="tech-card">
                            <img class="w-16 h-16 object-contain" src="assets/logos/javascript-original.svg" alt="javascript logo" />
                            <span class="text-sm font-medium text-white mt-2">JavaScript</span>
                        </div>
                        <div class="tech-card">
                            <img class="w-16 h-16 object-contain" src="assets/logos/nodejs-original.svg" alt="nodejs logo" />
                            <span class="text-sm font-medium text-white mt-2">Node.js</span>
                        </div>
                        <!-- Duplicados para efecto infinito -->
                        <div class="tech-card">
                            <img class="w-16 h-16 object-contain" src="assets/logos/angular-original.svg" alt="angular logo" />
                            <span class="text-sm font-medium text-white mt-2">Angular</span>
                        </div>
                        <div class="tech-card">
                            <img class="w-16 h-16 object-contain" src="assets/logos/react-original.svg" alt="react logo" />
                            <span class="text-sm font-medium text-white mt-2">React</span>
                        </div>
                        <div class="tech-card">
                            <img class="w-16 h-16 object-contain" src="assets/logos/nextjs.svg" alt="next logo" />
                            <span class="text-sm font-medium text-white mt-2">Next.js</span>
                        </div>
                        <div class="tech-card">
                            <img class="w-16 h-16 object-contain" src="assets/logos/tailwindcss-original.svg" alt="tailwind logo" />
                            <span class="text-sm font-medium text-white mt-2">TailwindCSS</span>
                        </div>
                    </div>


                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .carousel-container {
        width: 100%;
        overflow: hidden;
    }

    .carousel-track {
        display: flex;
        flex-direction: column;
        gap: 2rem;
    }

    .carousel-row {
        display: flex;
        gap: 2rem;
        animation: scroll 30s linear infinite;
        width: fit-content;
    }

    .carousel-row[data-direction="left"] {
        animation: scroll-left 30s linear infinite;
    }

    .carousel-row:hover {
        animation-play-state: paused;
    }

    .tech-card {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 1.5rem;
        background: rgba(30, 41, 59, 0.1);
        border: 1px solid rgba(59, 130, 246, 0.2);
        border-radius: 1rem;
        min-width: 140px;
        transition: all 0.3s ease;
        backdrop-filter: blur(10px);
    }

    .tech-card:hover {
        transform: translateY(-8px) scale(1.05);
        background: rgba(30, 41, 59, 0.8);
        border-color: rgba(59, 130, 246, 0.5);
        box-shadow: 0 10px 40px rgba(59, 130, 246, 0.3);
    }

    .tech-card img {
        filter: drop-shadow(0 4px 8px rgba(59, 130, 246, 0.2));
        transition: filter 0.3s ease;
    }

    .tech-card:hover img {
        filter: drop-shadow(0 8px 16px rgba(59, 130, 246, 0.4));
    }

    @keyframes scroll {
        0% {
            transform: translateX(0);
        }
        100% {
            transform: translateX(-50%);
        }
    }

    @keyframes scroll-left {
        0% {
            transform: translateX(-50%);
        }
        100% {
            transform: translateX(0);
        }
    }

    @media (max-width: 768px) {
        .carousel-row {
            gap: 1rem;
        }

        .tech-card {
            min-width: 100px;
            padding: 1rem;
        }

        .tech-card img {
            width: 3rem !important;
            height: 3rem !important;
        }
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const rows = document.querySelectorAll('.carousel-row');
        
        rows.forEach(row => {
            const items = row.querySelectorAll('.tech-card');
            items.forEach(item => {
                const clone = item.cloneNode(true);
                row.appendChild(clone);
            });
        });
    });
</script>